# 福格行为教练小程序 — 项目方案 v1

> 用户与"福格行为模型专家 Agent"在小程序内对话，专家诊断后生成个人行为改变计划，小程序承载每日执行、打卡与庆祝反馈。

## 1. 产品概述

**一句话**：AI 行为设计教练，把 BJ Fogg 的《福格行为模型 / Tiny Habits》方法论产品化。

**核心闭环**：
```
用户说愿望 → 专家引导诊断（B=MAP）→ 生成微习惯配方计划
→ 每日锚点提醒 → 一键打卡 → 即时庆祝(Shine) → 数据回流
→ 专家定期复盘 → 迭代计划（扩展/调整/解坏习惯）
```

**目标用户**：想养成习惯但屡败于意志力的普通人（健身、学习、作息、戒瘾）。

---

## 2. 专家 Agent 设计（核心资产）

### 2.1 会话流程（状态机）

```
[S1 探索愿望] → [S2 澄清现状] → [S3 头脑风暴] → [S4 黄金行为筛选]
→ [S5 设计配方] → [S6 确认计划] → [S7 生成Plan JSON] → 进入执行期
执行期：随时可回聊 → [S8 复盘迭代] / [S9 解坏习惯模式]
```

各阶段专家要做的事（写进 system prompt 的行为规范）：

| 阶段 | 专家行为 | 依据 |
|------|---------|------|
| S1 探索愿望 | 不问"目标"问"愿望"（"你希望生活有什么不同？"），共情式倾听，一次只问一个问题 | 七步法第1步 |
| S2 澄清现状 | 摸清三要素：动机来源（想/不想，波动规律）、能力障碍（时间/金钱/体力/脑力/日程五链）、现有作息锚点 | B=MAP + 能力链 |
| S3 头脑风暴 | 针对愿望给出 5-8 个候选行为（含发散选项），不评判 | 七步法第2步 |
| S4 黄金行为 | 用焦点地图引导用户选出：有效果×想做×能做 的交集，1-3 个 | 焦点地图 |
| S5 设计配方 | 每个黄金行为 → "在我[锚点]之后，我将[微行为]"，微行为必须 30 秒内可完成；教用户选庆祝方式 | 锚点配方 |
| S6 确认计划 | 复述计划，确认用户"感觉对"，强调错了可以改、不是承诺而是实验 | 设计心态 |
| S7 输出 JSON | 调用结构化输出，生成 plan JSON（见 3.1） | — |
| S8 复盘迭代 | 看打卡数据：全勤→建议扩展（"从2个俯卧撑到5个"）；缺勤→先查提示、再查难度、最后才谈动机 | 迭代原则 |
| S9 解坏习惯 | 走"解绳结"流程：先移除提示→再增加难度→最后调动机，从最容易的入手 | 第7章 |

### 2.2 对话规范

- 语气：温暖、不说教、简短（单次回复 ≤150 字，口语化）
- 铁律：
  - 永远不指责用户"缺乏意志力"，失败=设计问题
  - 不给宏大目标，一切落到 30 秒微行为
  - 每个阶段结束才推进，不跳步
  - 用户想跳过流程时先顺从再拉回
- 上下文：携带 plan 状态 + 最近 7 天打卡数据注入 system prompt（执行期）

### 2.3 System Prompt 结构

```
[角色] 你是"福格教练"，斯坦福行为设计实验室方法论专家...
[方法论内核] B=MAP / 行动线 / 能力链 / 焦点地图 / 锚点配方 / Shine / 解绳结（浓缩版全文）
[当前会话阶段] S3（由后端注入）
[用户计划状态] 无 / 现有 plan JSON（后端注入）
[打卡摘要] 近7天完成率（后端注入，执行期）
[输出要求] 当前阶段的行为规范 + JSON 输出触发词说明
```

---

## 3. 技术架构

### 3.1 数据模型

```sql
users        (id, openid, nickname, created_at)
sessions     (id, user_id, stage ENUM S1-S9, status, created_at)
messages     (id, session_id, role, content, created_at)
plans        (id, user_id, status ENUM active/paused/finished,
              version, created_at)
-- plan JSON 结构：
{
  "wish": "每天更有精神",
  "habits": [{
    "id": "h1",
    "anchor": "刷完牙之后",          -- 锚点
    "behavior": "做2个俯卧撑",        -- 微行为
    "celebration": "握拳说Yes",      -- 庆祝方式
    "anchor_time": "07:30",          -- 提醒时间
    "progression": [                 -- 扩展阶梯
      {"after_days": 7, "behavior": "做5个俯卧撑"}
    ]
  }],
  "review_day": "sunday"             -- 复盘日
}
checkins     (id, plan_id, habit_id, date, done BOOL,
              mood TINYINT 1-5, note, created_at)
```

### 3.2 后端 API（Node.js + Express/Fastify）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/chat` | POST | 对话。入参：sessionId, content。后端拼 system prompt + 历史 → LLM 流式 → SSE 返回。阶段推进由后端根据对话判断（LLM 输出 stage 标记） |
| `/api/plan/generate` | POST | S6 确认后触发。LLM structured output 生成 plan JSON，入库 |
| `/api/plan` | GET | 当前用户激活计划 |
| `/api/checkin` | POST | 打卡 `{habitId, done, mood}` |
| `/api/stats` | GET | 连续天数(streak)、7天完成率、日历热力图数据 |
| `/api/remind/bind` | POST | 绑定订阅消息授权（微信推送打卡提醒） |

- **LLM 层**：provider 抽象（OpenAI 兼容格式），可切换 SenseNova / GLM / DeepSeek
- **流式**：微信小程序 `wx.request` + `enableChunked` 接收 SSE
- **部署**：现有服务器，Nginx 反代 + HTTPS（宝塔），进程用 pm2

### 3.3 小程序页面（原生框架，不用云开发）

```
TabBar 4 个（2026-09-10 定稿，详见 docs/pages-interaction.md）：
├── today/       今日页(默认落地)。按plan分组配方卡，两档打卡：
│                轻打卡=点击即完成+Shine粒子光效+微震动；
│                记录打卡=可选面板(文字/图片≤3/mood)，默认可跳过
├── chat/        对话页。诊断期阶段进度条+气泡流+流式打字+QUICK chips；
│                执行期显示打卡摘要
├── community/   动态页(弱社区)。打卡记录可分享子集，默认不分享；
│                文字/图片先过微信机审(msgSecCheck/imgSecCheck)；
│                仅"加油"点赞无评论
└── me/          我的。首屏=统计(热力图/streak/完成率/心情曲线/复盘报告)，
                 其后提醒设置、计划历史、合规声明
plan/            计划详情(today二级页)。配方三段式卡片+streak+扩展预告
```

**打卡庆祝动画（Shine）是产品灵魂**：
- 打卡成功 → 全屏彩带粒子 + "你做到了！" + 每日不同鼓励语
- 动画必须 200ms 内响应（即时性是福格核心：情绪要紧跟行为）

**微信订阅消息**：plan 里每个 habit 的 anchor_time 推送提醒（提示P）。需用户主动订阅授权。

### 3.4 目录结构

```
fogg-coach/
├── server/            # 后端
│   ├── src/
│   │   ├── routes/    # api 路由
│   │   ├── llm/       # provider 抽象 + prompts/
│   │   │   └── prompts/fogg-expert.md   # 专家 system prompt（核心资产）
│   │   ├── db/        # sqlite + dao
│   │   └── app.js
│   └── package.json
├── miniprogram/       # 小程序前端（微信开发者工具导入）
│   ├── pages/
│   ├── app.json
│   └── ...
├── docs/
│   └── fogg-method.md # 福格方法论完整知识库（喂给agent的素材）
└── project-plan.md    # 本文档
```

---

## 4. 开发里程碑

| 阶段 | 内容 | 产出 | 预估 |
|------|------|------|------|
| M1 | 专家 prompt + 方法论文档 + 后端 /chat /plan | curl 能完整走完 S1-S7 出 plan JSON | 先做，1次会话 |
| M2 | 小程序端：chat + plan + today 三页 | 测试号可跑通全流程 | 2-3次会话 |
| M3 | 打卡 + 庆祝动画 + stats + 订阅消息提醒 | 完整 MVP | 2次会话 |
| M4 | 复盘迭代(S8) + 解坏习惯(S9) + 打磨 | 提审上线 | 后续 |
| v2 | 团队打卡（打卡圈：邀请制 ≤10人、圈内互见、圈子战报）+ 语音相关 | 见 system-design §3.3.7 | 已定稿不排期 |

**验证标准**（每阶段）：
- M1：用 3 个不同愿望（健身/早起/戒手机）测对话，均能产出合法 plan JSON
- M2：手机上完整走一遍"聊→生成→看到计划"
- M3：连续 3 天自用打卡，庆祝动画秒级响应

---

## 5. 风险与决策点

| 风险 | 应对 |
|------|------|
| LLM 对话跑偏/不按阶段走 | prompt 中强约束 + 后端校验 stage 推进 + 每阶段单独小 prompt 切换 |
| 微信小程序审核（AI 类目） | 个人主体需选"教育/工具"类目，对话内容避免医疗心理治疗表述；企业主体更稳 |
| 订阅消息一次性授权 | 每次打卡后引导重新订阅，降级方案：应用内提醒 |
| 用户聊一半流失 | chat 页支持"继续上次对话"，阶段进度条给续聊动力 |
| UGC 社区审核（个人主体） | 弱社区形态（无评论仅点赞、默认不分享），全部内容先过 msgSecCheck/imgSecCheck，有机审延迟等待态 |
| 永远在诊断从不执行 | 同 session RESET_WISH ≤2 次，第 3 次引导先执行 |
| 多 plan 并行失败率升高 | active plan ≥3 时教练软性引导收敛（不硬限） |
| 主动推荐"戒XX"漂向成瘾治疗 | S9 仅用户主动触发，不自动推荐（审核红线） |

---

## 6. 待定项

- [x] LLM 选型：**多 Provider 并存**，必须支持 **GLM-5.2** 和 **DeepSeek**（OpenAI 兼容协议抽象，热切/可按用途路由），2026-09-10 拍板
- [x] 小程序主体：**个人主体**（2026-09-10 拍板）。类目选 教育>学习辅导 或 工具，规避医疗/心理治疗表述；对话内容合规约束写入 prompt
- [x] 产品名：**挂起到提审前**（2026-09-10 拍板）。代码代号 fogg-coach；小程序对用户展示名做成**配置项**（app config / 后端下发），随时可改
